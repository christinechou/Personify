angular.module('narrative.sentiment', [])
.factory('Sentiment', function($http) {
  //refactor and move out to own folder
  // return $http({
  //   method: ''
  // })
})

.controller('SentimentCtrl', function($scope, $window, $location) {
  // $scope.data = {};
  // Sentiment.getAll()
})
